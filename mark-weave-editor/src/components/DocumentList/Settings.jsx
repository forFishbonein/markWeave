import React from 'react';

const Settings = () => (
  <div style={{ maxWidth: 400 }}>
    <h3>Settings</h3>
    <p>This is settings content (can be expanded based on actual needs).</p>
  </div>
);

export default Settings;
